# Crossover Pseudo-Grade

add the description of making the data

## Usage

``` r
crossover.pseudo.grades(grades, crossover.point = "random")
```

## Arguments

- grades:

  data.frame with the pseudo-grades

- crossover.point:

  integer indicating the assignment crossover point

## Value

data.frame with the crossovered (or is it crossed-over) grades

## Author

Emilio Xavier Esposito <emilio.esposito@gmail.com>
(<https://github.com/emilioxavier>)

## Examples

``` r
if (FALSE) { # \dontrun{
 crossover.pseudo.grades(grades=grades, crossover.point=5)
 crossover.pseudo.grades(grades=grades, crossover.point="random")
} # }
```
